# Test doc for viam-cloud-build markdown_link collection

This file exists to verify that build.sh discovers markdown_link references
in meta.json and uploads them to GCS at the sibling .model_docs/ prefix.

If you can read this in fake-gcs, it worked.
